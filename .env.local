# Environment
DJANGO_ENVIRONMENT=development

# Email Configuration - Development-friendly options
# Option 1: Console output (emails are printed to console)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Option 2: File-based (emails are saved to files) - Uncomment if preferred
# EMAIL_BACKEND=django.core.mail.backends.filebased.EmailBackend
# EMAIL_FILE_PATH=./sent_emails

# Option 3: Real SMTP like home.pl - Uncomment when you need to test actual delivery
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=serwer1733295.home.pl
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=faktury@xbrands.pl
EMAIL_HOST_PASSWORD='1Le1C7UF%$#%'
DEFAULT_FROM_EMAIL=Newsletter-Testowy-Faktury <faktury@xbrands.pl>

# Database Configuration - SQLite for local development
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# Django Secret Key - This is fine for local development
SECRET_KEY='django-insecure-_q8b04$s)zdkupvc3i415t6x#liela%!5$0p7nzpz%k08ypoi#'

# Debug settings
DEBUG=True

# Allowed Hosts
ALLOWED_HOSTS=localhost,127.0.0.1

SITE_URL=http://localhost:8000